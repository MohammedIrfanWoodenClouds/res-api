const r=require('express').Router();const c=require('../controllers/superAdminController');const {authenticate,requireRole}=require('../middleware/auth');r.use(authenticate,requireRole('SUPER_ADMIN'));r.get('/restaurants',c.list);r.post('/restaurants',c.create);r.put('/restaurants/:id',c.update);r.patch('/restaurants/:id/toggle',c.toggle);r.post('/restaurants/:id/reset-login',c.resetLogin);r.delete('/restaurants/:id',c.remove);module.exports=r;
